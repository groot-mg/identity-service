spring:
  jmx:
    enable: false
  application:
    name: identity-ft
  profiles:
    active: local

service:
  host: http://localhost:8181
#  context-path: /identity

wiremock:
  host: localhost
  port: 8180